version: '3'
services:
  bggapi:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    image: bggapi:latest
    ports:
      - "8051:8051"
    volumes:
      - ./bggapi:/app/bggapi
